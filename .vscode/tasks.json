{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build SASS",
            "type": "shell",
            "command": "sass ./sass/src:./css --update --style compressed --no-source-map",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Build SASS and watch",
            "type": "shell",
            "command": "sass ./sass/src:./css --update --style compressed --no-source-map --watch",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}